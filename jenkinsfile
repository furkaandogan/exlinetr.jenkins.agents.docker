@Library("gallifrey") _

docker_image_build_push_pipeline {
  application=[
    name:'jenkins-docker-agent',
    version:'1.0'
  ]
  jenkins=[
    agent:[
        label:'exlinetr-docker-slave'
    ]
  ]
  docker=[
    build:[
      remote:[
        address:'tcp://docker-agent.exlinetr.com:2376',
        credentialId:'exlinetr-docker-agent-01'
      ]
    ],
    hub:[
        address:
    ]
  ]
}
